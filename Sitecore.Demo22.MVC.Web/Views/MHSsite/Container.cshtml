@using Sitecore.Mvc
@using Sitecore.Mvc.Presentation
@model RenderingModel

@if (Model != null && Model.PageItem != null)
{
<div class="MHSABOUT">
    <h1>
        @Html.Sitecore().Field("Heading", Model.Item)
    </h1>

    <p>
        @Html.Sitecore().Field("Paragraph", Model.Item)
    </p>
    </div>
    }
